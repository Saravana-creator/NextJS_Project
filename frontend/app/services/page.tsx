import { GenericPage } from "@/components/layout/generic-page";

export default function ServicesPage() {
  return (
    <GenericPage
      eyebrow="Services"
      title="Dental services library"
      description="Service cards and treatment flows are ready, with no mock service data seeded yet."
    />
  );
}
