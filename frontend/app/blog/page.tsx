import { GenericPage } from "@/components/layout/generic-page";

export default function BlogPage() {
  return (
    <GenericPage
      eyebrow="Blog"
      title="Dental education hub"
      description="Article listing and future CMS boundaries are ready, with no mock posts seeded."
    />
  );
}
