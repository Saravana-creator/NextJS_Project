import { GenericPage } from "@/components/layout/generic-page";

export default function GalleryPage() {
  return (
    <GenericPage
      eyebrow="Gallery"
      title="Before and after results"
      description="Image slots are ready. No gallery mock images are included until you provide them."
    />
  );
}
