import { DashboardPage } from "@/components/layout/dashboard-page";

export default function AdminMessagesPage() {
  return <DashboardPage mode="admin" eyebrow="Inbox" title="Messages" description="Inbox and message detail views are ready for contact messages." />;
}
