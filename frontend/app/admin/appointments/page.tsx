import { DashboardPage } from "@/components/layout/dashboard-page";

export default function AdminAppointmentsPage() {
  return <DashboardPage mode="admin" eyebrow="Management" title="Appointments" description="Calendar, table, approval, rejection, and reschedule views are ready." />;
}
